name: Continuous Integration
on: push

jobs:
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Version check
        run: go version
      - name: Install dependencies
        run: go mod tidy
      - name: Run unit tests
        run: go test ./...
